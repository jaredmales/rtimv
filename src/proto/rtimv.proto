syntax="proto3";

package remote_rtimv;

service rtimv
{
    rpc Configure(Config) returns (ConfigResult);

    rpc LoadColorbar(Colorbar) returns (ColorbarLightness);

    rpc ImagePlease(ImageRequest) returns (Image);

    rpc GetPixel(Coord) returns(Pixel);

//    rpc ColorBox(Box) returns (MindatMaxdat);

    rpc StatsBox(Box) returns(Pixel);

}

message Config
{
    string file = 1;
    string image_key = 2;
    string dark_key = 3;
    string flat_key = 4;
    string mask_key = 5;
    string sat_mask_key = 6;

// change numbers

    bool update_timeout_set = 9;
    uint32 update_timeout = 10;

    bool update_cube_fps_set = 11;
    float update_cube_fps = 12;

    bool autoscale_set = 13;
    bool autoscale = 14;

    bool darksub_set = 15;
    bool darksub = 16;

    bool satlevel_set = 17;
    float satlevel = 18;

    bool mask_sat_set = 19;
    bool mask_sat = 20;

    bool mzmq_always_set = 21;
    bool mzmq_always = 22;

    string mzmq_server = 23;

    bool mzmq_port_set = 24;
    uint32 mzmq_port = 25;
}

message ConfigResult
{
    int32 result = 1;
}

//Send a request to set the colorbar.
// If -1 is sent, then this is a query to get the current color bar.
message Colorbar
{
    int32 colorbar = 1;
}

message ColorbarLightness
{
    int32 colorbar = 1; // The colorbar for which this lightness vector applies
    bytes lightness = 2; // The lightness vector for the colorbar
}

message ImageRequest
{
}

enum ImageStatus
{
    IMAGE_STATUS_VALID = 0;
    IMAGE_STATUS_NO_IMAGE = 1;
    IMAGE_STATUS_TIMEOUT = 2;
    IMAGE_STATUS_NOT_CONFIGURED = 3;
    IMAGE_STATUS_ERROR = 4;
}

message Image
{
    ImageStatus status = 1;

    uint32 nx = 10; // x-size of source image (not size of jpeg)
    uint32 ny = 11; // y-size of source image (not size of jpeg)
    uint32 nz = 12; // z-size of source image

    uint32 no = 20; // image number within the cube

    double atime = 30; // acquisition time of source image
    double fps = 31; // FPS of stream (measured)

    bytes image = 100; // The image data

    uint32 saturated = 105; // The number of saturated pixels

    float min_image_data = 110;
    float max_image_data = 111;
    float min_scale_data = 112;
    float max_scale_data = 113;
}

message Coord
{
    uint32 x = 1;
    uint32 y = 2;
}

message Pixel
{
    bool valid = 1;
    float value = 2;
}


message Box
{
    Coord upperLeft = 1;
    Coord lowerRight = 2;
}
